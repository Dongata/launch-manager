{
  "name": "launch-manager",
  "version": "1.0.0",
  "description": "something",
  "main": "app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc && copyfiles app/**/*.json dist/",
    "start": "node ./bin/www.js"
  },
  "author": "Gaston Cerioni",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.11.0",
    "copyfiles": "^1.2.0",
    "dependency-injection": "^2.3.3",
    "dependency-injection.ts": "^1.0.3",
    "express": "^4.16.2",
    "rimraf": "^2.6.2",
    "typescript": "^2.6.2"
  }
}
